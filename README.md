# SQL-Project-
Real-world SQL project analyzing 25,000 'Netflix users' covers WHERE, AND, OR, NOT, ORDER BY, LIMIT, DISTINCT, Aggregate Functions, GROUP BY, HAVING, INNER JOIN and LEFT JOIN using DB Browser for SQLite. 
